export enum Roles {
    admin = 'admin',
    manager = 'manager',
    employee = 'employee'
}

export enum PermissionLevel{
    all ='all',
    group='group',
    own='own',
}

export enum Actions{
    create='create',
    read='read',
    update='update',
    delete='delete'
}

